// Define a variable count with original value of 0 and then increment it
// (add 1 to it) on the following line.

let count = 0;
++ count;
console.log(count);

const ageLimit = 18;
console.log(ageLimit);